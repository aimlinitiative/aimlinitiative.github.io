rules_version = '2';

// Allow public reads for course materials (guides, etc.)
// Only authenticated users can write
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public reads for course materials
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
